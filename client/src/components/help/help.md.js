export default
`
# Bookmarks Archive

## Introduction

Bookmarks Archive (*BA*) is desktop program to store your Web Bookmarks. Over time number of bookmarks, which you save for later use or which you want to read later will grow and it is not convenient to keep them in browser bookmarks.

*BA* allows to manage your bookmarks in easy and convenient way.

## Functionality

You can import bookmarks from any major browser. First export bookmarks as HTML file. Then use that HTML file to import bookmarks into *BA*. You can import bookmarks like that a few times, each bookmark will be imported just once.

If bookmark is located is some sub-folder this information will be saved in *original path* field in bookmark.

You can edit bookmarks, create tags, assign tags to bookmarks.

There are different filtering modes. You can limit bookmarks to new ones (with no tags), bookmarks with particular tags (or condition) or deleted bookmarks.

For each filter mode you can search by text. It will search in bookmark *title*, *url* and *original path*.

## Tips

There are some keyboard shortcuts:

* Ctrl+Enter: to save bookmark and close popup modal
* Esc: close modal or close tag list selection

It maybe convenient to use tags names like folder structure:

* Programming/JavaScript
* Programming/JavaScript/ES6
* Programming/CSS
`